import { DevicesState } from '@app/devices/store/devices.state';

import { ApplicationState } from './app.store';
import { SettingsState } from '@app/settings/store/settings.state';

export const States = [ApplicationState, DevicesState, SettingsState];
